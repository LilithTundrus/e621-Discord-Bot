import * as Discord from 'discord.js';

// per server with setchannel active, we need to cache the file URLs
// that they've already 'seen'

export function initScheduler(client: Discord.Client) {
    // read the user/channel manifest

    // add a listener for each channel with their specialprefs/set of 'unseen' images
    
}